# awasero-music 備考

開発補助AI向けの引き継ぎメモ。次回開発時に参照すること。表に出す機能一覧やユーザー向け説明は`README.md`・`実装計画書.md`を参照。

## v1.0.0で対応したこと

- 精査で見つかった全項目（機能の確実性・安定性・UIの完全性）を修正・実装した。詳細は`実装計画書.md`の「実装済み機能」を参照。
- 単体テスト(`Tests/AwaseroMusicTests`)を新規追加。`swift test`はフルXcodeが必要（`DEVELOPER_DIR`参照、README参照）。

## 既知の未対応・今後の課題

1. **ピッチ検出アルゴリズム**: 自己相関ベース（粗密2段探索で高速化済み）のまま。YIN/MPM系への置換は未着手。
2. **異名同音スペリング**: キーの♯/♭優先度による2分岐のみ。スケール上の役割（属七の導音など）を考慮した完全な異名同音表記ではない。
3. **MusicXMLの完全性**: 休符の明示挿入、小節充填、タイの表記が未対応。音符が拍を跨ぐ場合や無音区間があるケースで、譜面ソフト側の解釈に依存する可能性がある。
4. **PDF出力のテンポ曲線**: 複数テンポポイントはMIDI/MusicXML/WAVには反映されるが、PDF譜面上には最初のテンポのみ表示（視覚的な複数テンポ表示は未実装）。
5. **入力機器選択の実機検証**: `AVAudioEngine`＋CoreAudioでのデバイス切り替えロジックは実装したが、開発環境に複数の物理マイクがなく、実機での切り替え動作は未検証。複数マイクを接続した環境での確認が必要。
6. **Undo/Redoの粒度**: キーストローク単位ではなく、音符選択の切り替えや明示的な操作単位でのスナップショット方式。デバッグビルドでの動作は確認済みだが、大量編集時のメモリ使用量（スナップショット50件上限）は未検証。
7. **評価データセット**: 実鼻歌・ノイズ付き音声を使った回帰評価、正解データ、合格基準は未整備。
8. **Developer ID署名・公証**: v1.0.0はad-hoc署名で配布。公開配布時はApple Developerアカウントでの署名・公証が必要。

## 配布・インフラ

- GitHubリポジトリ: `oriyu90/awasero-music`（Private）
- 紹介Webサイト: Cloudflare Pages（Git連携）、`website/`ディレクトリを配信。ダウンロード導線はDiscordへの誘導のみ（GitHub Releaseがprivateのため）。将来リポジトリ/リリースをPublicにする場合は`website/index.html`のダウンロードリンク1箇所を差し替える。
- ライセンス: MIT、著作者 Yuki_Orita
