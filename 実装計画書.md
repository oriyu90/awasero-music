# 合わせろMusic（awasero-music）実装計画書

## 1. 製品方針

単旋律の鼻歌を録音後に解析し、音同士の相対関係を保ちながら楽譜・MIDIへ変換することに集中する。解析結果とユーザー編集結果を分離し、元録音を破壊せず再解析できる構成を維持する。

伴奏機能は、伴奏音源の生成ではなく、曲に合うコード名とコード進行の提示に限定する。

## 2. 非搭載機能

以下は現在も将来も搭載対象にしない。

- 録音中のリアルタイム譜面化・MIDI出力
- 多声・和音・複数歌唱者の解析
- DAWとのリアルタイム同期
- 内蔵歌声合成
- AIによる楽曲生成

MIDIやMusicXMLを書き出し、ユーザーが別アプリで利用する通常のファイル連携は対象とする。

## 3. 実装済み機能

- macOS SwiftUIアプリ基盤
- プロジェクト、録音、解析結果、楽譜、コードのデータモデル
- プロジェクト一覧（開く・複製・削除）、自動保存・クラッシュ復旧
- 入力機器の選択（AVAudioEngineベースの録音、システム既定入力は変更しない）
- マイク録音、確認再生、入力レベル表示、無音・クリッピング警告、録音時間上限
- BPM・拍子・カウントイン付き録音メトロノーム
- オフラインの基本周波数解析（キャンセル可能、粗密2段探索による高速化）
- 発音区間の抽出、テンポ推定、音符量子化
- 調候補ごとに最適な全体ピッチオフセットを探索する調推定
- ピアノロール、簡易五線譜、音符編集、複数選択
- 範囲選択によるベロシティ一括設定・クレッシェンド/デクレッシェンド
- 複数テンポポイントの追加・編集（書き出しに反映）、拍範囲を指定したテンポの微調整
- タップテンポ入力（TAP/リセット）
- 選択音符の半音単位の移調
- 音符のクオンタイズ（グリッド・強さ・終了位置スナップの指定）
- Undo/Redo、再解析、手動編集の破棄確認
- 内蔵音色によるメロディ試聴
- コード進行の推定、表示、手動修正（MIDI・MusicXMLへ反映）
- 調号・臨時記号を反映したMusicXML/PDF出力
- プロジェクト保存・読込
- MIDI、MusicXML、PDF、WAV出力
- 単体テスト（`swift test`）
- MITライセンスとDMG配布処理

## 4. 今後の品質改善

### 4.1 解析精度

1. YIN/MPM系ピッチ検出への置換または併用
2. ビブラート、しゃくり、ブレス、倍音誤検出の抑制
3. 原音波形とピッチ曲線の重ね表示
4. 実鼻歌の正解データセットを使った回帰評価

### 4.2 編集機能

1. 休符の明示編集、音符の分割・結合
2. 五線譜の改行、連桁、タイの改善
3. スケール上の役割まで考慮した完全な異名同音スペリング（現状はキーの♯/♭優先度による簡易分岐）

### 4.3 ファイル互換性

1. MusicXMLの休符、小節充填、タイ、音価表記の完全化
2. MIDI、MusicXML、PDFを主要な音楽制作アプリで開く互換性試験
3. プロジェクト形式の移行テスト

## 5. テスト計画

- 音高変換、調・コード推定、出力形式を自己診断（`--self-check`）と単体テスト（`swift test`）で検査する。
- 合成音（正弦波・複数ピッチ）を使った解析の回帰テストを実装済み。実鼻歌・ノイズ付き音声を使った評価は今後の課題とする。
- 録音権限拒否、無音入力、極端に短い入力、解析キャンセルを異常系としてテスト済み。入力切断・保存容量不足は今後の課題とする。
- macOSの最低対応バージョンと最新バージョンで起動・録音・書き出しを確認する。

## 6. 配布計画

- ライセンス: MIT License
- 著作者: Yuki_Orita
- 配布形式: `合わせろMusic-<version>.dmg`
- DMG内容: アプリ本体、Applicationsへのショートカット、LICENSE、README
- 通常ビルド: ad-hoc署名
- 公開配布: Apple Developer ID署名とApple公証を推奨

## 7. リリース完了条件

1. Release構成でビルドできる。
2. アプリのコード署名とInfo.plistが検証を通る。
3. 自己診断がすべて成功する。
4. DMGをマウントでき、想定した4項目が含まれる。
5. DMGのSHA-256チェックサムを配布できる。
6. LICENSEとアプリ内著作権表示がYuki_Orita名義になっている。
