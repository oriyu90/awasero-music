# 合わせろMusic（awasero-music）簡易設計書

## 1. 文書概要

### 1.1 目的

「合わせろMusic」（英語名: `awasero-music`）は、Macのマイクで録音した鼻歌を解析し、メロディを楽譜とMIDIに変換する作曲支援アプリである。本書はその基本設計を定める。

本アプリは、鼻歌の各音を単純に半音単位へ丸めるのではなく、音同士の相対関係を保ちながら、曲全体に適した調とピッチ補正量を推定することを特徴とする。

### 1.2 想定ユーザー

- 音楽理論や楽譜入力に詳しくないが、思いついたメロディを残したい人
- 鼻歌をMIDI化し、ほかの音楽制作アプリで利用したい人
- メロディに合うコード進行を知りたい初級～中級の作曲者

### 1.3 対象環境

- 対応OS: macOS
- 入力: Mac内蔵マイク、またはmacOSで選択可能な音声入力機器
- 主な出力: MIDIファイル
- その他の出力: MusicXML、PDF楽譜、音声ファイル

## 2. 開発範囲

### 2.1 実装対象

鼻歌を「録る・解析する・直す・書き出す」という中心体験を軸に、以下を実装する。

1. 鼻歌の録音、再生、録り直し
2. メトロノームを聴きながらの録音
3. 録音後の高精度なオフライン解析
4. 単旋律のピッチ、発音区間、休符、テンポの推定
5. 調・スケール候補と全体ピッチオフセットの推定
6. 音符データへの変換と五線譜・ピアノロール表示
7. 音程、音価、休符、テンポ、拍子、調、オクターブの編集
8. 範囲指定によるテンポ、強弱、ベロシティ編集
9. メロディの内蔵音色による試聴
10. 曲に合うコード進行の推定と表示（伴奏音源の自動生成は行わない）
11. Standard MIDI File、MusicXML、PDF楽譜、音声ファイルの書き出し
12. プロジェクトの保存と再編集

### 2.2 非搭載機能

本アプリは単旋律の録音後解析に集中する。録音中の譜面化・MIDI出力、多声解析、DAWとの同期、内蔵歌声合成、AI楽曲生成は搭載しない。

## 3. 機能一覧

| ID | 機能 | 概要 | 提供段階 |
|---|---|---|---|
| F-01 | プロジェクト管理 | 新規作成、保存、複製、削除、再読込 | 基盤 |
| F-02 | 入力機器選択 | 使用するマイクと入力レベルを設定 | 基盤 |
| F-03 | 鼻歌録音 | メトロノーム、録音、停止、再生、録り直し | 基盤 |
| F-04 | 音声解析 | 基本周波数、音量、発音区間を抽出 | 基盤 |
| F-05 | 調・音程補正 | 調候補と共通ピッチオフセットを推定 | 基盤 |
| F-06 | 音符変換 | ピッチ列を音符、音価、休符へ変換 | 基盤 |
| F-07 | 楽譜表示 | 五線譜・ピアノロールに変換結果を表示 | 対象 |
| F-08 | メロディ編集 | 音程、音価、休符、拍子等を修正 | 基盤 |
| F-09 | 試聴 | メロディを内蔵音源で再生 | 基盤 |
| F-10 | MIDI出力 | 編集結果をMIDIファイルへ書き出し | 基盤 |
| F-11 | コード推定 | 曲に合うコード進行を推定して表示 | 対象 |
| F-12 | 表現編集 | 強弱、ベロシティ、テンポカーブを編集 | 対象 |
| F-13 | その他の出力 | MusicXML、PDF、音声を書き出し | 対象 |

## 4. 画面構成

### 4.1 プロジェクト一覧画面

- 新規プロジェクト作成
- 保存済みプロジェクトの一覧、更新日時、長さの表示
- プロジェクトを開く、複製する、削除する

### 4.2 録音画面

- 入力機器の選択
- 入力レベルメーター
- 録音開始・停止ボタン
- BPM、拍子、カウントインを設定できるメトロノーム
- 録音時間と波形の表示
- 録音内容の再生、録り直し、解析開始
- 無音や入力過大などの警告

### 4.3 解析結果・編集画面

- 上部: 再生、停止、テンポ、拍子、調の操作
- 中央: 波形と楽譜またはピアノロール
- 下部または右側: 選択音符の音程、開始位置、長さ、ベロシティ
- 調候補と信頼度の表示、候補の切り替え
- 元音声、補正後メロディの切り替え試聴
- 元に戻す、やり直す、再解析

### 4.4 書き出し画面

- 出力形式の選択（MIDI、MusicXML、PDF楽譜、音声）
- テンポ、拍子、書き出し範囲の確認
- ファイル名と保存先の指定

### 4.5 コード画面

- 推定コード進行と小節ごとのコード名
- 複数候補と信頼度の表示
- コードの修正、再推定、テキスト／MusicXML／MIDIへの反映
- 伴奏演奏データや伴奏音源そのものは生成しない

## 5. 基本操作フロー

```text
新規プロジェクト
  ↓
マイクとメトロノームを設定して鼻歌を録音
  ↓
録音内容を確認
  ↓
ピッチ・リズム・調を解析
  ↓
候補を選択して音符へ変換
  ↓
楽譜／ピアノロール上で修正・試聴
  ↓
プロジェクト保存
  ↓
MIDIを書き出してDAW等で利用
```

解析に失敗した場合は、録り直しに加えて、テンポ・拍子・調をユーザーが指定して再解析できるようにする。

## 6. システム構成案

アプリ内の責務を以下の6層に分離する。解析方式や表示方式を後から交換しやすくするため、音声解析結果と編集後の楽曲データは別に保持する。

| 層・モジュール | 主な責務 |
|---|---|
| UI | 録音、波形・楽譜表示、編集、書き出し操作 |
| Project | プロジェクトの作成、保存、読込、バージョン管理 |
| Audio | マイク入力、録音、音声再生、音声ファイル管理 |
| Analysis | ピッチ追跡、発音区間検出、テンポ・拍推定、調推定 |
| Music Model | 音符、休符、小節、テンポ、拍子、調、コードを表現 |
| Export | MIDI・MusicXML・PDF・音声出力 |

### 6.1 推奨技術構成

- UI: SwiftUI
- 音声入出力: AVFoundation / AVAudioEngine
- 音楽再生: AVAudioUnitSampler または AudioKit等の採用を別途評価
- データ保存: JSON形式のプロジェクトメタデータ＋録音音声ファイル
- MIDI入出力: Core MIDI、またはMIDIファイル生成ライブラリ
- 楽譜表示: 初期版はピアノロールを優先し、五線譜ライブラリの採用は検証後に決定
- 解析処理: Swift内のDSP実装、またはPython等で検証したアルゴリズムを移植

外部ライブラリは、ライセンス、Apple Silicon対応、保守状況、オフライン動作を確認して選定する。

## 7. 音声解析設計

### 7.1 解析パイプライン

1. 音声のモノラル化とサンプルレート統一
2. ノイズ低減、音量正規化、無音区間の検出
3. フレーム単位の基本周波数と信頼度の抽出
4. 不安定区間、ブレス、子音、倍音誤検出の除去
5. 連続する安定ピッチを発音区間としてまとめる
6. 発音開始位置からテンポと拍位置を推定
7. 調・スケール候補ごとの適合度を計算
8. 全発音区間へ共通ピッチオフセットを適用して再評価
9. 最も自然な候補を音符と休符へ量子化
10. 候補、信頼度、補正前後の情報を保存

### 7.2 調と全体ピッチオフセットの推定

- 各音を個別に最寄りの半音へ丸める前に、音同士の音程差と旋律の輪郭を評価する。
- 複数の調・スケール候補に対し、共通ピッチオフセットを少しずつ変えて適合度を計算する。
- 評価には、スケール内音の割合、安定音の滞在時間、終止音、音程差の保存度、ピッチ信頼度を用いる。
- 上位候補を複数表示し、ユーザーが選択または手動指定できるようにする。
- 信頼度が低い音は自動確定せず、編集画面で識別できる表示にする。

### 7.3 リズム推定

- 発音開始位置と発音時間から拍間隔を推定する。
- 複数のテンポ候補（例: 倍テン、半テン）を比較する。
- 拍子は初期版では4/4を既定値とし、3/4等の候補提示と手動変更に対応する。
- 量子化の細かさは固定せず、8分音符・16分音符等を選択可能にする。

## 8. データ設計

### 8.1 主なデータ

| データ | 主な項目 |
|---|---|
| Project | ID、名称、作成日時、更新日時、アプリ・形式バージョン |
| Recording | 音声ファイル参照、長さ、サンプルレート、入力機器 |
| AnalysisResult | フレーム別ピッチ、信頼度、発音区間、解析設定 |
| Score | テンポマップ、拍子、調、小節、メロディトラック |
| Note | ID、開始位置、長さ、MIDIノート番号、ベロシティ、信頼度 |
| TempoEvent | 適用位置、BPM、変化方法 |
| ChordProgression | 候補名、小節位置、コード名、構成音、信頼度 |

### 8.2 保存構成例

```text
ProjectName.hummingproject/
├── project.json
├── original-audio.m4a
├── analysis.json
└── preview/            # 必要に応じて生成する一時データ
```

- 元録音は再解析のために保持する。
- 編集済みのScoreと元のAnalysisResultを分離する。
- 保存形式にバージョン番号を持たせ、将来の移行に備える。
- 自動生成可能なプレビューやキャッシュは、失われても復元できる設計とする。

## 9. 主なルールと例外処理

- 録音時間の初期上限は5分を目安とし、性能検証後に確定する。
- マイク権限がない場合は、理由とmacOS設定への案内を表示する。
- 入力音量が小さすぎる、クリッピングする、無音が長い場合は録音画面で警告する。
- ピッチ検出の信頼度が低い区間は、休符扱いまたは要確認として提示する。
- 解析をキャンセルしても録音データは保持する。
- 解析結果を再生成する前に、手動編集を保持するか破棄するか確認する。
- MIDI出力時はテンポ、拍子、ノート開始位置、長さ、ベロシティを反映する。

## 10. 非機能要件

### 10.1 性能

- 録音操作への反応は体感上遅延しないこと。
- 3分程度の録音を一般的なApple Silicon搭載Macで実用的な待ち時間内に解析すること。
- 解析中は進捗とキャンセル操作を表示し、画面を停止させないこと。

### 10.2 品質

- 元音声を破壊せず、何度でも異なる設定で再解析できること。
- 編集操作は元に戻す・やり直すに対応すること。
- 自動保存またはクラッシュ復旧用の一時保存を行うこと。

### 10.3 プライバシー

- 初期版は録音と解析を端末内で完結させる。
- 録音データを外部送信する機能を追加する場合は、送信前に明示的な同意を得る。
- マイクの利用目的を権限要求前にわかりやすく説明する。

### 10.4 アクセシビリティ

- 主要操作をキーボードでも実行可能にする。
- 色だけに依存せず、アイコンやラベルでも解析信頼度を伝える。
- VoiceOverで主要ボタンと音符編集項目を識別可能にする。

## 11. 中核機能の受け入れ条件

1. ユーザーがMacのマイクで単旋律の鼻歌を録音できる。
2. 録音終了後に解析を開始し、進捗と失敗理由を確認できる。
3. 解析結果が音符と休符として時系列に表示される。
4. 調の上位候補と補正結果を切り替えて試聴できる。
5. ユーザーが各音の音程と長さを修正できる。
6. 編集内容を保存し、アプリ再起動後も再編集できる。
7. 書き出したMIDIを一般的なDAWで開き、同じテンポとメロディで再生できる。
8. 不明瞭な録音でもアプリが異常終了せず、録り直しまたは手動修正へ進める。

## 12. 開発ステップ案

1. 録音・再生とプロジェクト保存の試作
2. 単音ピッチ検出の技術検証と評価用音声の準備
3. 発音区間、テンポ、音符量子化の実装
4. 調・全体ピッチオフセット推定の実装
5. ピアノロール表示、編集、試聴
6. MIDI出力と主要DAWでの互換性確認
7. 五線譜表示の検証
8. コード進行推定の実装
9. MusicXML・PDF・音声出力と配布パッケージ作成

## 13. 未決事項

- 最低対応macOSバージョン
- 楽譜表示を初期版から五線譜にするか、ピアノロールを優先するか
- 採用するピッチ検出アルゴリズムと外部ライブラリ
- テンポ・拍子の自動推定精度と手動入力の優先順位
- プロジェクトの最大録音時間とファイル容量
- MIDI以外の初期出力形式
- 解析精度を測る評価データ、正解データ、合格基準
- Developer ID署名・公証を行う場合のApple Developerアカウント

## 14. 成功指標案

- 初回利用者が説明なしで録音からMIDI出力まで完了できる割合
- 正解音高に対する音符単位の一致率
- ユーザーが手動修正した音符の割合
- 解析開始から編集可能になるまでの時間
- 書き出したMIDIをDAWで正常に利用できた割合

これらの基準値は、試作版で実際の鼻歌データを収集した後に決定する。
